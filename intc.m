function [y] = intc(x,a,b)
%This function integrates a constant between bounds a and b. 
%Dimensionons: NA
%Assumptions: Integrand is a constant.
%Syntax: int_c(constnat,bound 1, bound 2)

y=x*(b-a);

end

